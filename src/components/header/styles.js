import styled from "styled-components";

export const Header = styled.nav`
  position: relative;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    padding: .5rem 1rem;
    background: #000;
    padding: 30px 45px;


    .logo {
      width: 180px;
      height: auto;
    }
`